<Window x:Class="SudokuSolver.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:SudokuControl;assembly=SudokuControl"
        mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Button Grid.Column="0" Grid.Row="0" Margin="5">Load from file</Button>
        <Button Grid.Column="1" Grid.Row="0" Margin="5">Enter as string</Button>
        <local:SudokuControl Grid.Column="0" Grid.Row="1" Grid.ColumnSpan="2" Margin="5"/>
        <Button Grid.Column="0" Grid.Row="2" Grid.ColumnSpan="2" Margin="5">Solve</Button>
    </Grid>
</Window>
